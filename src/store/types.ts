export interface Product {
  _id: string;
  title: string;
  imageUrl: string;
  category: string;
  price: number;
  discount: number;
  rating: number;
  description: string;
  stock: number;
  // add other product properties
}
